/**
 * Exports the `createDBClient` function from the `GlobalOrbit` module.
 * This function creates a new instance of the DBClient, allowing external applications to interact with the database.
 *
 * @returns {Object} An instance of DBClient.
 */
import createDBClient from "./GlobalOrbit";

export default createDBClient;
